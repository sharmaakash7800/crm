{
  "name": "sheetless-crm",
  "version": "1.0.0",
  "description": "Custom CRM & Lead Management System - Google Sheets Replacement",
  "main": "server/index.js",
  "scripts": {
    "server": "node server/index.js",
    "client": "npm --prefix client run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm install && npm --prefix client install && npm --prefix client run build",
    "render-build": "npm install && npm --prefix client install && npm --prefix client run build"
  },
  "keywords": [
    "crm",
    "lead-management",
    "sheet-replacement",
    "pipeline",
    "followup"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.2",
    "cors": "^2.8.5",
    "csv-parser": "^3.2.0",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "mongoose": "^9.9.5",
    "multer": "^1.4.5-lts.1",
    "nodemon": "^3.1.9",
    "xlsx": "^0.18.5"
  }
}
